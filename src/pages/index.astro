---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Collections 가져오기
const publications = await getCollection('publications');
const projects = await getCollection('projects');
const blogPosts = await getCollection('blog');

// Recent publications (최신 3개)
const recentPubs = publications
  .filter(p => p.data.status === 'published')
  .sort((a, b) => b.data.year - a.data.year)
  .slice(0, 3);

// Featured projects (최신 3개)
const featuredProjects = projects
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
  .slice(0, 3);

// Recent blog posts (최신 3개)
const recentPosts = blogPosts
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
  .slice(0, 3);

// 통계
const totalCitations = publications.reduce((sum, pub) => sum + (pub.data.citations || 0), 0);
const ssciPapers = publications.filter(p => p.data.type === 'ssci').length;
---

<BaseLayout title="Home" description="Professor of Business Administration specializing in media policy, digital platforms, and AI governance research">
  <div class="homepage">
    <header class="hero">
      <h1 class="name">김용희 (YONGHEE KIM)</h1>
      <p class="subtitle">Assistant Professor of Business Administration | Media Policy & Digital Platform Research</p>
    </header>

    <section class="bio">
      <p>
        I'm an Assistant Professor at <a href="https://mgt.sunmoon.ac.kr/mgt/ad/pp/profsrintrcnPage.do?profsrSe=A&mi=41267" target="_blank">Sunmoon University</a>, specializing in media policy, digital platform regulation, and AI governance research. My work focuses on Korean broadcasting policy, K-Content IP sovereignty, digital platform governance, and statistical analysis methodologies.
      </p>
      <p>
        My research has been published in leading SSCI journals including <em>Telecommunications Policy</em> (IF 5.6, Q1), <em>Journal of Retailing and Consumer Services</em> (IF 13.1, Q1), and <em>Sustainability</em>. With over {totalCitations} citations, my work on IoT adoption and technology acceptance has become a foundational reference in the field.
      </p>
      <p>
        I actively contribute to policy discourse through research presentations at the National Assembly and expert commentary in <a href="https://www.etnews.com/" target="_blank">The Electronic Times</a>. My research emphasizes <strong>reproducible methods</strong> and <strong>open data</strong>, with all code and datasets publicly available for verification and replication.
      </p>
      <p>
        In recognition of my contributions to media policy research, I've received commendations from the Korea Communications Commission and the Ministry of Science and ICT. I also serve as Director of the Daeyeon Industrial Policy Institute and am a cofounder of <a href="https://www.openroute.kr/" target="_blank">OpenRoute</a>.
      </p>
    </section>

    <section class="section ksap-highlight">
      <div class="section-header">
        <h2>K-Survey Analytics Platform (KSAP)</h2>
        <a href="https://github.com/yhkim1981-svg/KSAP" target="_blank" rel="noopener" class="section-link">View on GitHub →</a>
      </div>
      
      <div class="ksap-content">
        <p class="ksap-intro">
          A Korean-language statistical analysis platform designed as an open-source alternative to SPSS, SmartPLS, and PROCESS. 
          KSAP combines Python FastAPI with R statistical computing to provide comprehensive tools for quantitative research.
        </p>
        
        <div class="ksap-features">
          <div class="feature-card">
            <h4>Basic Statistics</h4>
            <p>t-tests, ANOVA, Regression with full diagnostics</p>
          </div>
          <div class="feature-card">
            <h4>MCDM Methods</h4>
            <p>AHP, ANP, ISM, DEMATEL, TOPSIS</p>
          </div>
          <div class="feature-card">
            <h4>SEM & PLS</h4>
            <p>Structural Equation Modeling</p>
          </div>
          <div class="feature-card">
            <h4>R Integration</h4>
            <p>Leveraging professional R packages via rpy2</p>
          </div>
        </div>

        <div class="ksap-status">
          <div class="status-item">
            <span class="status-label">Status</span>
            <span class="status-value">Phase 1.5 - Advanced Regression</span>
          </div>
          <div class="status-item">
            <span class="status-label">Tech Stack</span>
            <span class="status-value">Python FastAPI + R 4.3+</span>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Code & Data</h2>
        <a href="https://github.com/trustune" target="_blank" rel="noopener" class="section-link">View All Repositories →</a>
      </div>
      
      <div class="code-data-list">
        <article class="code-item">
          <a href="https://github.com/trustune/broadcasting-disclosure-db" target="_blank" rel="noopener" class="code-link">
            <div class="code-header">
              <h3 class="code-title">Korean Broadcasting Industry Database</h3>
            </div>
            <p class="code-description">
              Comprehensive SQLite database (tv_integrated.db) containing 2015-2024 Korean broadcasting industry data. 
              Includes revenue, investment, employment, and regulatory data for reproducible media policy research.
            </p>
            <div class="code-meta">
              <span class="code-tag">SQLite</span>
              <span class="code-tag">Panel Data</span>
              <span class="code-tag">1,500+ observations</span>
            </div>
          </a>
        </article>

        <article class="code-item">
          <a href="https://kimyonghee.com/api" class="code-link">
            <div class="code-header">
              <h3 class="code-title">Research Data API</h3>
            </div>
            <p class="code-description">
              JSON/CSV APIs providing structured access to publications, projects, and research metadata. 
              Supports R, Python, and Excel for immediate analysis.
            </p>
            <div class="code-meta">
              <span class="code-tag">JSON API</span>
              <span class="code-tag">CSV Export</span>
              <span class="code-tag">Open Access</span>
            </div>
          </a>
        </article>

        <article class="code-item">
          <a href="https://github.com/yhkim1981-svg/KSAP" target="_blank" rel="noopener" class="code-link">
            <div class="code-header">
              <h3 class="code-title">KSAP Source Code</h3>
            </div>
            <p class="code-description">
              Open-source statistical analysis platform. All analysis scripts, documentation, 
              and development roadmap available for community contribution.
            </p>
            <div class="code-meta">
              <span class="code-tag">Python</span>
              <span class="code-tag">R</span>
              <span class="code-tag">Open Source</span>
            </div>
          </a>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Recent Research</h2>
        <a href="/research/publications" class="section-link">View All Publications →</a>
      </div>
      
      <div class="publication-list">
        {recentPubs.map(pub => (
          <article class="publication-item">
            <a href={`/research/${pub.slug}`} class="pub-link">
              <h3 class="pub-title">{pub.data.title}</h3>
              <div class="pub-meta">
                <span class="pub-authors">{pub.data.authors.join(', ')}</span>
                <span class="separator">•</span>
                <span class="pub-journal">{pub.data.journal}</span>
                <span class="separator">•</span>
                <span class="pub-year">{pub.data.year}</span>
                {pub.data.impact_factor && (
                  <>
                    <span class="separator">•</span>
                    <span class="pub-if">IF {pub.data.impact_factor}</span>
                  </>
                )}
                {pub.data.citations > 0 && (
                  <>
                    <span class="separator">•</span>
                    <span class="pub-citations">{pub.data.citations} citations</span>
                  </>
                )}
              </div>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Featured Projects</h2>
        <a href="/projects" class="section-link">View All Projects →</a>
      </div>
      
      <div class="project-list">
        {featuredProjects.map(project => (
          <article class="project-item">
            <a href={`/projects/${project.slug}`} class="project-link">
              <div class="project-header">
                <h3 class="project-title">{project.data.title}</h3>
                <span class="project-category">{project.data.category}</span>
              </div>
              <p class="project-description">{project.data.description}</p>
              <div class="project-meta">
                <span class="project-date">{new Date(project.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short' })}</span>
                {project.data.tags && project.data.tags.length > 0 && (
                  <div class="project-tags">
                    {project.data.tags.slice(0, 3).map(tag => (
                      <span class="tag">{tag}</span>
                    ))}
                  </div>
                )}
              </div>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Latest Blog Posts</h2>
        <a href="/blog" class="section-link">View All Posts →</a>
      </div>
      
      <div class="blog-list">
        {recentPosts.map(post => (
          <article class="blog-item">
            <a href={`/blog/${post.slug}`} class="blog-link">
              <div class="blog-meta">
                <span class="blog-category">{post.data.category}</span>
                <span class="separator">•</span>
                <span class="blog-date">{new Date(post.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
              </div>
              <h3 class="blog-title">{post.data.title}</h3>
              <p class="blog-excerpt">{post.data.excerpt}</p>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="section">
      <div class="section-header">
        <h2>Connect</h2>
      </div>

      <nav class="links">
        <a href="mailto:yhkim1981@sunmoon.ac.kr" class="link-item">
          <span class="link-title">Email</span>
          <span class="link-arrow">→</span>
        </a>
        <a href="https://github.com/yhkim1981-svg" target="_blank" rel="noopener" class="link-item">
          <span class="link-title">GitHub</span>
          <span class="link-arrow">→</span>
        </a>
        <a href="https://scholar.google.com/citations?user=semkeskAAAAJ" target="_blank" rel="noopener" class="link-item">
          <span class="link-title">Google Scholar</span>
          <span class="link-arrow">→</span>
        </a>
        <a href="https://bsky.app/profile/hastara.bsky.social" target="_blank" rel="noopener" class="link-item">
          <span class="link-title">Bluesky</span>
          <span class="link-arrow">→</span>
        </a>
      </nav>
    </section>
  </div>
</BaseLayout>

<style>
  .homepage { max-width: 1200px; margin: 0 auto; padding: 4rem 2rem; }
  
  .hero { margin-bottom: 4rem; text-align: center; }
  .name { 
    font-family: 'Paperozi', 'Merriweather', serif; 
    font-size: 3rem; 
    font-weight: 300; 
    letter-spacing: -0.02em; 
    color: #1e40af; 
    margin-bottom: 0.75rem; 
  }
  .subtitle { 
    font-size: 1.25rem; 
    color: #6b7280; 
    margin: 0 0 2rem 0; 
  }
  
  .stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
    background: #f9fafb;
    border-radius: 1rem;
  }
  
  .stat-item {
    text-align: center;
  }
  
  .stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: 700;
    color: #1e40af;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  
  .stat-label {
    display: block;
    font-size: 0.875rem;
    color: #6b7280;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .bio {
    margin-bottom: 4rem;
    padding-bottom: 3rem;
    border-bottom: 1px solid #e5e7eb;
    font-size: 1.125rem;
    line-height: 1.8;
  }
  .bio p {
    margin-bottom: 1.75rem;
    text-align: justify;
    text-justify: inter-word;
  }
  .bio p:last-child { margin-bottom: 0; }
  .bio a {
    color: #2563eb;
    text-decoration: underline;
    text-underline-offset: 0.25em;
    text-decoration-thickness: 1.5px;
  }
  .bio a:hover { color: #1e40af; }
  .bio em { font-style: italic; color: #374151; font-weight: 500; }
  .bio strong { font-weight: 700; color: #1e40af; }
  
  .section { 
    margin-bottom: 4rem; 
    padding-bottom: 3rem; 
    border-bottom: 1px solid #e5e7eb; 
  }
  .section:last-child { border-bottom: none; }
  
  .section-header { 
    display: flex; 
    justify-content: space-between; 
    align-items: baseline; 
    margin-bottom: 2rem; 
  }
  .section-header h2 { 
    font-family: 'Paperozi', 'Merriweather', serif; 
    font-size: 2rem; 
    font-weight: 400; 
    margin: 0; 
    color: #1e40af;
  }
  .section-link { 
    font-size: 0.875rem; 
    color: #6b7280; 
    text-decoration: none; 
    text-transform: uppercase; 
    letter-spacing: 0.05em; 
  }
  .section-link:hover { color: #2563eb; }
  
  /* KSAP Highlight Section */
  .ksap-highlight {
    background: linear-gradient(135deg, #eff6ff 0%, #f9fafb 100%);
    border: 2px solid #2563eb;
    border-radius: 1rem;
    padding: 2rem !important;
  }
  
  .ksap-intro {
    font-size: 1.125rem;
    line-height: 1.7;
    color: #4b5563;
    margin-bottom: 2rem;
  }
  
  .ksap-features {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin-bottom: 2rem;
  }
  
  .feature-card {
    background: white;
    padding: 1.5rem;
    border-radius: 0.75rem;
    text-align: center;
    border: 1px solid #e5e7eb;
    transition: all 0.2s;
  }
  
  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.1);
    border-color: #2563eb;
  }

  .feature-card h4 {
    font-size: 1rem;
    font-weight: 700;
    color: #1e40af;
    margin-bottom: 0.75rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid #e5e7eb;
  }
  
  .feature-card p {
    font-size: 0.875rem;
    color: #6b7280;
    line-height: 1.5;
  }
  
  .ksap-status {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    background: white;
    padding: 1.25rem;
    border-radius: 0.5rem;
    border: 1px solid #e5e7eb;
  }
  
  .status-item {
    display: flex;
    flex-direction: column;
  }
  
  .status-label {
    font-size: 0.75rem;
    color: #6b7280;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.25rem;
  }
  
  .status-value {
    font-size: 0.875rem;
    font-weight: 600;
    color: #1e40af;
  }
  
  /* Code & Data Section */
  .code-data-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  
  .code-link {
    text-decoration: none;
    display: block;
    padding: 1.5rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    transition: all 0.2s;
    background: white;
  }
  
  .code-link:hover {
    border-color: #2563eb;
    background: #eff6ff;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }
  
  .code-header {
    margin-bottom: 0.75rem;
  }

  .code-title {
    font-family: 'Paperozi', 'Inter', sans-serif;
    font-size: 1.25rem;
    font-weight: 600;
    color: #1f2937;
    line-height: 1.4;
    border-left: 3px solid #2563eb;
    padding-left: 1rem;
  }
  
  .code-description {
    color: #4b5563;
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  
  .code-meta {
    display: flex;
    gap: 0.75rem;
  }
  
  .code-tag {
    background: #f3f4f6;
    color: #4b5563;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
  }
  
  /* Publications */
  .publication-list { display: flex; flex-direction: column; gap: 1.5rem; }
  .publication-item { }
  .pub-link { 
    text-decoration: none; 
    display: block;
    padding: 1.5rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    transition: all 0.2s;
  }
  .pub-link:hover {
    border-color: #2563eb;
    background: #eff6ff;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }
  .pub-title { 
    font-family: 'Paperozi', 'Inter', sans-serif; 
    font-size: 1.25rem; 
    font-weight: 500; 
    margin-bottom: 0.75rem; 
    line-height: 1.4; 
    color: #1f2937;
  }
  .pub-meta { 
    font-size: 0.875rem; 
    color: #6b7280; 
    display: flex; 
    flex-wrap: wrap; 
    gap: 0.5rem; 
  }
  .separator { color: #d1d5db; }
  .pub-if { font-weight: 600; color: #059669; }
  .pub-citations { font-weight: 600; color: #7c3aed; }
  
  /* Projects */
  .project-list { display: grid; gap: 1.5rem; }
  .project-item { }
  .project-link {
    text-decoration: none;
    display: block;
    padding: 1.5rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    transition: all 0.2s;
  }
  .project-link:hover {
    border-color: #2563eb;
    background: #eff6ff;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }
  .project-header {
    display: flex;
    justify-content: space-between;
    align-items: start;
    margin-bottom: 0.75rem;
  }
  .project-title {
    font-family: 'Paperozi', 'Inter', sans-serif;
    font-size: 1.25rem;
    font-weight: 500;
    color: #1f2937;
    line-height: 1.4;
    flex: 1;
  }
  .project-category {
    font-size: 0.75rem;
    color: #2563eb;
    background: #eff6ff;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .project-description {
    color: #4b5563;
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  .project-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.875rem;
    color: #6b7280;
  }
  .project-tags {
    display: flex;
    gap: 0.5rem;
  }
  .tag {
    background: #f3f4f6;
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
    font-size: 0.75rem;
  }
  
  /* Blog */
  .blog-list { display: flex; flex-direction: column; gap: 1.5rem; }
  .blog-item { }
  .blog-link {
    text-decoration: none;
    display: block;
    padding: 1.5rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    transition: all 0.2s;
  }
  .blog-link:hover {
    border-color: #2563eb;
    background: #eff6ff;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }
  .blog-meta {
    font-size: 0.875rem;
    color: #6b7280;
    margin-bottom: 0.5rem;
  }
  .blog-category {
    font-weight: 600;
    color: #2563eb;
  }
  .blog-title {
    font-family: 'Paperozi', 'Inter', sans-serif;
    font-size: 1.25rem;
    color: #1f2937;
    font-weight: 500;
    line-height: 1.4;
    margin-bottom: 0.5rem;
  }
  .blog-excerpt {
    color: #4b5563;
    line-height: 1.6;
  }
  
  /* Links */
  .links {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(2, 1fr);
  }
  .link-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.125rem 1.5rem;
    border: 2px solid #e5e7eb;
    border-radius: 0.5rem;
    text-decoration: none;
    color: #1f2937;
    transition: all 0.2s;
    background: white;
  }
  .link-item:hover {
    border-color: #2563eb;
    background-color: #eff6ff;
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }
  .link-title {
    font-weight: 600;
    font-size: 1rem;
  }
  .link-arrow {
    color: #2563eb;
    font-weight: 600;
    font-size: 1.25rem;
  }
  
  /* Tablet */
  @media (max-width: 1024px) {
    .homepage { padding: 3rem 1.5rem; }
    .name { font-size: 2.5rem; }
    .ksap-features {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Mobile */
  @media (max-width: 768px) {
    .homepage { padding: 2.5rem 1rem; }

    .hero { margin-bottom: 3rem; }
    .name {
      font-size: 1.75rem;
      line-height: 1.2;
      margin-bottom: 1rem;
    }
    .subtitle {
      font-size: 0.95rem;
      line-height: 1.6;
      max-width: 100%;
    }

    .bio {
      font-size: 1rem;
      line-height: 1.9;
      margin-bottom: 3rem;
      padding-bottom: 2.5rem;
    }
    .bio p {
      margin-bottom: 2rem;
      text-align: left;
    }

    .section {
      margin-bottom: 3rem;
      padding-bottom: 2.5rem;
    }

    .section-header {
      flex-direction: column;
      gap: 1rem;
      align-items: start;
      margin-bottom: 1.75rem;
    }
    .section-header h2 {
      font-size: 1.5rem;
    }
    .section-link {
      font-size: 0.8125rem;
    }

    .stats {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
      padding: 1.5rem;
    }
    .stat-number { font-size: 1.75rem; }

    .ksap-highlight {
      padding: 1.5rem !important;
      margin-bottom: 3rem;
    }
    .ksap-intro {
      font-size: 1rem;
      line-height: 1.8;
      margin-bottom: 1.5rem;
    }
    .ksap-features {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    .feature-card {
      padding: 1.25rem;
    }
    .ksap-status {
      grid-template-columns: 1fr;
    }

    /* Cards */
    .code-link,
    .pub-link,
    .project-link,
    .blog-link {
      padding: 1.25rem;
    }

    .code-title,
    .pub-title,
    .project-title,
    .blog-title {
      font-size: 1.125rem;
      line-height: 1.5;
    }

    .code-description,
    .project-description,
    .blog-excerpt {
      font-size: 0.9375rem;
      line-height: 1.7;
    }

    /* Links */
    .links {
      grid-template-columns: 1fr;
    }
    .link-item {
      padding: 1.25rem 1.5rem;
    }
  }

  /* Small Mobile */
  @media (max-width: 640px) {
    .homepage { padding: 2rem 1rem; }

    .hero { margin-bottom: 2.5rem; }
    .name {
      font-size: 1.5rem;
      letter-spacing: -0.01em;
    }
    .subtitle {
      font-size: 0.875rem;
      line-height: 1.7;
    }

    .bio {
      font-size: 0.9375rem;
      line-height: 2;
      margin-bottom: 2.5rem;
    }
    .bio p {
      margin-bottom: 2.25rem;
    }

    .section {
      margin-bottom: 2.5rem;
      padding-bottom: 2rem;
    }

    .section-header {
      margin-bottom: 1.5rem;
    }
    .section-header h2 {
      font-size: 1.375rem;
    }

    .stats {
      grid-template-columns: 1fr;
      gap: 1.25rem;
      padding: 1.25rem;
    }
    .stat-number { font-size: 1.5rem; }
    .stat-label { font-size: 0.8125rem; }

    .ksap-highlight {
      padding: 1.25rem !important;
    }
    .feature-card {
      padding: 1rem;
    }
    .feature-card h4 {
      font-size: 0.9375rem;
    }
    .feature-card p {
      font-size: 0.8125rem;
    }

    .code-link,
    .pub-link,
    .project-link,
    .blog-link {
      padding: 1rem;
    }

    .code-title,
    .pub-title,
    .project-title,
    .blog-title {
      font-size: 1.0625rem;
    }

    /* Links */
    .links {
      grid-template-columns: 1fr;
    }
    .link-item {
      padding: 1.125rem 1.25rem;
    }
  }
</style>
