---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

// Markdown projects 가져오기
const allProjects = await getCollection('projects');

// 날짜 역순 정렬 (최신이 위로)
const projects = allProjects
  .map(project => ({
    id: project.slug,
    ...project.data,
  }))
  .sort((a, b) => new Date(b.date) - new Date(a.date));

// 카테고리 추출
const categories = [...new Set(projects.map(p => p.category))];

// 연도별 그룹핑
const projectsByYear = projects.reduce((acc, project) => {
  const year = new Date(project.date).getFullYear();
  if (!acc[year]) acc[year] = [];
  acc[year].push(project);
  return acc;
}, {});

const years = Object.keys(projectsByYear).sort((a, b) => b - a);
---

<BaseLayout title="Projects - Yonghee Kim" description="Research projects and data analysis work">
  <div class="projects-container">
    <header class="page-header">
      <h1 class="page-title" data-lang="en">Research Projects</h1>
      <h1 class="page-title" data-lang="ko">연구 프로젝트</h1>
      <p class="page-description" data-lang="en">
        Data-driven research projects and policy analysis work
      </p>
      <p class="page-description" data-lang="ko">
        데이터 기반 연구 프로젝트 및 정책 분석 작업
      </p>

      <!-- Stats -->
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-value">{projects.length}</div>
          <div class="stat-label" data-lang="en">Total Projects</div>
          <div class="stat-label" data-lang="ko">전체 프로젝트</div>
        </div>
        <div class="stat-card">
          <div class="stat-value">{categories.length}</div>
          <div class="stat-label" data-lang="en">Categories</div>
          <div class="stat-label" data-lang="ko">카테고리</div>
        </div>
        <div class="stat-card">
          <div class="stat-value">{years.length}</div>
          <div class="stat-label" data-lang="en">Years</div>
          <div class="stat-label" data-lang="ko">연도</div>
        </div>
      </div>
    </header>

    <!-- Filter Controls -->
    <div class="filter-controls">
      <div class="filter-group">
        <label class="filter-label" data-lang="en">Category</label>
        <label class="filter-label" data-lang="ko">카테고리</label>
        <select id="categoryFilter" class="filter-select">
          <option value="all" data-lang="en">All Categories</option>
          <option value="all" data-lang="ko">전체 카테고리</option>
          {categories.map(cat => (
            <option value={cat}>{cat}</option>
          ))}
        </select>
      </div>

      <div class="filter-group">
        <label class="filter-label" data-lang="en">Year</label>
        <label class="filter-label" data-lang="ko">연도</label>
        <select id="yearFilter" class="filter-select">
          <option value="all" data-lang="en">All Years</option>
          <option value="all" data-lang="ko">전체 연도</option>
          {years.map(year => (
            <option value={year}>{year}</option>
          ))}
        </select>
      </div>

      <div class="filter-group">
        <label class="filter-label" data-lang="en">Sort By</label>
        <label class="filter-label" data-lang="ko">정렬</label>
        <select id="sortFilter" class="filter-select">
          <option value="date-desc" data-lang="en">Newest First</option>
          <option value="date-desc" data-lang="ko">최신순</option>
          <option value="date-asc" data-lang="en">Oldest First</option>
          <option value="date-asc" data-lang="ko">오래된 순</option>
        </select>
      </div>

      <button id="resetFilters" class="reset-btn">
        <span data-lang="en">Reset Filters</span>
        <span data-lang="ko">필터 초기화</span>
      </button>
    </div>

    <!-- Projects Grid -->
    <div class="projects-grid" id="projectsGrid">
      {projects.map((project) => (
        <article 
          class="project-card" 
          data-category={project.category}
          data-year={new Date(project.date).getFullYear()}
          data-date={project.date}
          data-featured={project.featured ? 'true' : 'false'}
        >
          {project.featured && (
            <div class="featured-badge">FEATURED</div>
          )}
          
          <div class="project-header">
            <div class="project-category-badge">{project.category}</div>
            <div class="project-date">
              {new Date(project.date).toLocaleDateString('en-US', { 
                year: 'numeric', 
                month: 'short', 
                day: 'numeric' 
              })}
            </div>
          </div>

          <h3 class="project-title">
            <a href={`/projects/${project.id}`}>
              <span data-lang="en">{project.title_en}</span>
              <span data-lang="ko">{project.title}</span>
            </a>
          </h3>

          {project.subtitle_en && (
            <p class="project-subtitle">
              <span data-lang="en">{project.subtitle_en}</span>
              <span data-lang="ko">{project.subtitle}</span>
            </p>
          )}

          <p class="project-summary">
            <span data-lang="en">{project.summary || project.description_en}</span>
            <span data-lang="ko">{project.summary || project.description}</span>
          </p>

          {project.conference && (
            <div class="project-conference">
              📍 {project.conference}
            </div>
          )}

          {project.key_findings && project.key_findings.length > 0 && (
            <div class="key-findings">
              <strong data-lang="en">Key Findings:</strong>
              <strong data-lang="ko">주요 발견:</strong>
              <ul>
                {project.key_findings.slice(0, 2).map((finding) => (
                  <li>{finding}</li>
                ))}
              </ul>
            </div>
          )}

          <div class="project-tags">
            {project.tags && project.tags.slice(0, 3).map(tag => (
              <span class="tag">{tag}</span>
            ))}
          </div>

          <a href={`/projects/${project.id}`} class="view-btn">
            <span data-lang="en">View Project →</span>
            <span data-lang="ko">프로젝트 보기 →</span>
          </a>
        </article>
      ))}
    </div>

    <!-- No Results Message -->
    <div id="noResults" class="no-results" style="display: none;">
      <p data-lang="en">No projects found matching your filters.</p>
      <p data-lang="ko">필터 조건에 맞는 프로젝트가 없습니다.</p>
    </div>
  </div>
</BaseLayout>

<style>
  .projects-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
  }

  .page-header {
    margin-bottom: 3rem;
    text-align: center;
  }

  .page-title {
    font-size: 3rem;
    color: #1e40af;
    margin-bottom: 1rem;
    font-weight: 800;
  }

  .page-description {
    font-size: 1.125rem;
    color: #6b7280;
    margin-bottom: 2rem;
  }

  /* Stats */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  .stat-card {
    padding: 1.5rem;
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
    border-radius: 1rem;
    text-align: center;
    border: 2px solid #bfdbfe;
  }

  .stat-value {
    font-size: 3rem;
    font-weight: 900;
    color: #1e40af;
    font-family: 'Inter', sans-serif;
  }

  .stat-label {
    font-size: 0.875rem;
    color: #6b7280;
    font-weight: 600;
    margin-top: 0.5rem;
  }

  /* Filters */
  .filter-controls {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    align-items: flex-end;
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: #f9fafb;
    border-radius: 0.5rem;
  }

  .filter-group {
    flex: 1;
    min-width: 200px;
  }

  .filter-label {
    display: block;
    font-size: 0.875rem;
    font-weight: 600;
    color: #374151;
    margin-bottom: 0.5rem;
  }

  .filter-select {
    width: 100%;
    padding: 0.625rem 1rem;
    border: 2px solid #e5e7eb;
    border-radius: 0.5rem;
    font-size: 0.9375rem;
    background: white;
    cursor: pointer;
    transition: all 0.2s;
  }

  .filter-select:hover {
    border-color: #2563eb;
  }

  .filter-select:focus {
    outline: none;
    border-color: #2563eb;
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  .reset-btn {
    padding: 0.625rem 1.5rem;
    background: #ef4444;
    color: white;
    border: none;
    border-radius: 0.5rem;
    font-size: 0.9375rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .reset-btn:hover {
    background: #dc2626;
  }

  /* Projects Grid */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .project-card {
    position: relative;
    padding: 2rem;
    background: white;
    border: 2px solid #e5e7eb;
    border-radius: 1rem;
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
  }

  .project-card:hover {
    border-color: #2563eb;
    box-shadow: 0 8px 24px rgba(37, 99, 235, 0.15);
    transform: translateY(-4px);
  }

  .project-card[data-featured="true"] {
    border-color: #2563eb;
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
  }

  .featured-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.375rem 0.75rem;
    background: #2563eb;
    color: white;
    font-size: 0.6875rem;
    font-weight: 700;
    border-radius: 9999px;
    letter-spacing: 0.05em;
  }

  .project-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .project-category-badge {
    padding: 0.375rem 0.75rem;
    background: #2563eb;
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    border-radius: 0.25rem;
  }

  .project-date {
    font-size: 0.875rem;
    color: #6b7280;
    font-weight: 500;
  }

  .project-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    line-height: 1.3;
  }

  .project-title a {
    color: #1f2937;
    text-decoration: none;
    transition: color 0.2s;
  }

  .project-title a:hover {
    color: #2563eb;
  }

  .project-subtitle {
    font-size: 1rem;
    color: #6b7280;
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .project-summary {
    font-size: 0.9375rem;
    color: #4b5563;
    line-height: 1.6;
    margin-bottom: 1rem;
    flex-grow: 1;
  }

  .project-conference {
    padding: 0.75rem;
    background: #fffbeb;
    border-left: 3px solid #f59e0b;
    font-size: 0.875rem;
    color: #78350f;
    margin-bottom: 1rem;
    border-radius: 0.25rem;
  }

  .key-findings {
    background: #f9fafb;
    padding: 1rem;
    border-radius: 0.5rem;
    margin-bottom: 1rem;
  }

  .key-findings strong {
    display: block;
    color: #1e40af;
    margin-bottom: 0.5rem;
    font-size: 0.875rem;
  }

  .key-findings ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .key-findings li {
    padding: 0.25rem 0;
    padding-left: 1rem;
    position: relative;
    color: #4b5563;
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .key-findings li::before {
    content: "▸";
    position: absolute;
    left: 0;
    color: #2563eb;
    font-weight: 700;
  }

  .project-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }

  .tag {
    padding: 0.25rem 0.625rem;
    background: #e5e7eb;
    color: #374151;
    font-size: 0.75rem;
    font-weight: 500;
    border-radius: 0.25rem;
  }

  .view-btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: #2563eb;
    color: white;
    text-decoration: none;
    border-radius: 0.5rem;
    font-weight: 600;
    font-size: 0.9375rem;
    text-align: center;
    transition: all 0.2s;
  }

  .view-btn:hover {
    background: #1e40af;
    transform: translateX(4px);
  }

  .no-results {
    text-align: center;
    padding: 4rem 2rem;
    color: #6b7280;
    font-size: 1.125rem;
  }

  @media (max-width: 768px) {
    .page-title {
      font-size: 2rem;
    }

    .projects-grid {
      grid-template-columns: 1fr;
    }

    .filter-controls {
      flex-direction: column;
    }

    .filter-group {
      width: 100%;
    }

    .reset-btn {
      width: 100%;
    }
  }
</style>

<script>
  const categoryFilter = document.getElementById('categoryFilter');
  const yearFilter = document.getElementById('yearFilter');
  const sortFilter = document.getElementById('sortFilter');
  const resetBtn = document.getElementById('resetFilters');
  const projectsGrid = document.getElementById('projectsGrid');
  const noResults = document.getElementById('noResults');

  function filterProjects() {
    const category = categoryFilter.value;
    const year = yearFilter.value;
    const sort = sortFilter.value;

    const cards = Array.from(projectsGrid.querySelectorAll('.project-card'));

    // Filter
    let visibleCount = 0;
    cards.forEach(card => {
      const cardCategory = card.dataset.category;
      const cardYear = card.dataset.year;

      const categoryMatch = category === 'all' || cardCategory === category;
      const yearMatch = year === 'all' || cardYear === year;

      if (categoryMatch && yearMatch) {
        card.style.display = 'flex';
        visibleCount++;
      } else {
        card.style.display = 'none';
      }
    });

    // Sort
    const visibleCards = cards.filter(card => card.style.display !== 'none');
    visibleCards.sort((a, b) => {
      const dateA = new Date(a.dataset.date);
      const dateB = new Date(b.dataset.date);
      return sort === 'date-asc' ? dateA - dateB : dateB - dateA;
    });

    visibleCards.forEach(card => projectsGrid.appendChild(card));

    // Show/hide no results
    noResults.style.display = visibleCount === 0 ? 'block' : 'none';
  }

  categoryFilter.addEventListener('change', filterProjects);
  yearFilter.addEventListener('change', filterProjects);
  sortFilter.addEventListener('change', filterProjects);

  resetBtn.addEventListener('click', () => {
    categoryFilter.value = 'all';
    yearFilter.value = 'all';
    sortFilter.value = 'date-desc';
    filterProjects();
  });
</script>
</BaseLayout>
